# Research Agent Configuration

search:
  # Test 1: GAN (2025 only)
  query: '(abs:"GAN" OR abs:"Generative Adversarial Network") AND submittedDate:[202501010000 TO 202512312359]'
  max_results: 3
  sort_by: "submitted_date" # options: submitted_date, relevance, last_updated_date
  sort_order: "descending" # options: descending, ascending

refiner:
  model_name: "models/gemini-flash-latest"
  temperature: 0.2
  top_p: 0.95
  max_output_tokens: 8192

paths:
  data_dir: "data/papers"
  history_file: "data/history.json"
  hugo_bin: "bin/hugo.exe"
  public_dir: "public"

publisher:
  branch: "gh-pages"
  remote: "origin"
  source_branch: "main"
